spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
#        server-addr: 192.168.40.100:1111
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
management:
  endpoints:
    web:
      exposure:
        include: '*'